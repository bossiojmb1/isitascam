services:
  - type: web
    name: isitascam-api
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 4000
